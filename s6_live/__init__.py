"""S6 breakout family: candidate interpretation for the shared cycle.

Decisions and records only. Nothing here places an order -- S6 joins
`run_live_buy_entry_cycle` as a SOURCE, exactly as S2 does, so every
safety gate below it is the same code S1 runs.
"""
